# Local planning / context — not for publication
supporting-docs/

# HF Space staging dir (assembled by space/assemble.sh)
space_build/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Data & models (large; canonical copies live on the HF Hub, not in git).
# Anchored to the repo root so it does NOT swallow site/data/ (the published open-data JSON).
/data/
models/
*.wav
*.flac
*.ogg
*.mp3
*.m4a
# Real-world benchmark: audio + the filled clip-list stay local (copyright / private-eval).
# The schema template (clips.example.csv) and the harness ARE tracked.
benchmark/realworld/audio/
benchmark/realworld/clips.csv
benchmark/realworld/links.csv
*.pkl
*.parquet
*.pt
*.onnx
*.tflite

# Env / secrets
.env
.env.local
*.hf_token

# Local tool caches / state (never publish)
.wrangler/
**/.wrangler/
.playwright-mcp/

# OS / editor
.DS_Store
.idea/
.vscode/
