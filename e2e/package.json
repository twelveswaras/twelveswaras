{
  "name": "twelveswaras-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser end-to-end user tests for the twelveswaras.com site (Playwright).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
